function lengthOfLongestSubstring(s: string): number {
  const lenghtOfSubString = 0;
  let subString = "";
  for (var char of s) {
    if (subString.toLowerCase().indexOf("stream") === -1
     && char.toLowerCase().indexOf("stream") === -1) {
        var a = "..."
    }
  }
  return lenghtOfSubString;
}
lengthOfLongestSubstring("aaccsd");
